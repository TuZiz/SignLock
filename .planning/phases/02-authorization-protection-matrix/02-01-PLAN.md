---
phase: 02-authorization-protection-matrix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/java/ym/signLock/listener/LockListenerBreakProtectionTest.java
  - src/test/java/ym/signLock/listener/LockListenerProtectionMatrixTest.java
  - src/test/java/ym/signLock/command/SignLockCommandAuthorizationTest.java
  - src/test/java/ym/signLock/service/PlayerIdentityServiceTest.java
autonomous: true
requirements: [PROT-03, PROT-04, OPS-02, OPS-03]
must_haves:
  truths:
    - "未授权玩家、已授权玩家、主人、管理员绕过在破坏路径上的边界可被自动化断言区分。"
    - "爆炸、活塞、流体、InventoryMove 自动化对受保护目标和受管理锁牌的影响有明确红绿测试。"
    - "`/signlock add`、`/signlock remove`、`/signlock info` 对无效目标、扩展牌满、目标未命中、大小写与改名场景有明确结果断言。"
    - "玩家身份缓存对 cache-known、cache-unknown、rename 三类场景有可重复的自动化证明。"
  artifacts:
    - path: "src/test/java/ym/signLock/listener/LockListenerBreakProtectionTest.java"
      provides: "PROT-03 listener regression contract"
      contains: "owner/authorized/admin/unauthorized block and sign break cases"
    - path: "src/test/java/ym/signLock/listener/LockListenerProtectionMatrixTest.java"
      provides: "PROT-04 matrix contract"
      contains: "inventory move, explosion, piston, fluid, managed sign coverage"
    - path: "src/test/java/ym/signLock/command/SignLockCommandAuthorizationTest.java"
      provides: "OPS-02 command outcome contract"
      contains: "add/remove/info invalid target, extension full, rename/case handling"
    - path: "src/test/java/ym/signLock/service/PlayerIdentityServiceTest.java"
      provides: "OPS-03 identity cache contract"
      contains: "remember, preload, resolveStoredName, findUuidByName"
  key_links:
    - from: "src/test/java/ym/signLock/listener/LockListenerBreakProtectionTest.java"
      to: "src/main/java/ym/signLock/listener/LockListener.java"
      via: "BlockBreakEvent assertions"
      pattern: "onBlockBreak"
    - from: "src/test/java/ym/signLock/listener/LockListenerProtectionMatrixTest.java"
      to: "src/main/java/ym/signLock/listener/LockListener.java"
      via: "event cancellation and affected-block filtering assertions"
      pattern: "onInventoryMove|onBlockExplode|onEntityExplode|onPistonExtend|onPistonRetract|onFluidFlow"
    - from: "src/test/java/ym/signLock/command/SignLockCommandAuthorizationTest.java"
      to: "src/main/java/ym/signLock/command/SignLockCommand.java"
      via: "command result and message assertions"
      pattern: "handleAdd|handleRemove|handleInfo"
    - from: "src/test/java/ym/signLock/service/PlayerIdentityServiceTest.java"
      to: "src/main/java/ym/signLock/service/PlayerIdentityService.java"
      via: "cache persistence and UUID-aware name resolution assertions"
      pattern: "preload|remember|findUuidByName|resolveStoredName"
---

<objective>
先把 Phase 2 的四组缺失回归套件补齐，作为后续权限矩阵与命令修复的红绿基线。

Purpose: 先证明风险，再改行为，避免在 `LockService` / listener / command 上引入新的语义漂移。
Output: 四个可直接执行的 Phase 2 回归测试文件，覆盖 break protection、protection matrix、command authorization、identity cache。
</objective>

<execution_context>
@C:/Users/32633/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/32633/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-authorization-protection-matrix/02-CONTEXT.md
@.planning/phases/02-authorization-protection-matrix/02-RESEARCH.md
@.planning/phases/01-lock-target-correctness/01-01-SUMMARY.md
@.planning/phases/01-lock-target-correctness/01-02-SUMMARY.md
@.planning/phases/01-lock-target-correctness/01-03-SUMMARY.md
@src/main/java/ym/signLock/service/LockService.java
@src/main/java/ym/signLock/listener/LockListener.java
@src/main/java/ym/signLock/command/SignLockCommand.java
@src/main/java/ym/signLock/service/PlayerIdentityService.java

<interfaces>
Current permission surface from `LockService`:

```java
public boolean canAccess(LockInfo lock, Player player)
public boolean canManage(LockInfo lock, Player player)
public boolean isExplosionProtected(Block block)
public boolean isProtectedAutomationTarget(Block block)
public AddPlayerResult addPlayerToLock(Sign preferredSign, LockInfo lock, String playerName)
public RemovePlayerResult removePlayerFromLock(Sign preferredSign, LockInfo lock, String playerName)
```

Current break path from `LockListener`:

```java
LockInfo directLock = lockService.findLock(block);
if (directLock != null && !lockService.canAccess(directLock, player)) { ... }

LockInfo signLock = lockService.findManagedSignLock(block);
if (signLock != null && !lockService.canAccess(signLock, player)) { ... }
```

Current command identity asymmetry from `SignLockCommand`:

```java
targetPlayer = plugin.getPlayerIdentityService().resolveStoredName(targetPlayer); // add only
RemovePlayerResult result = plugin.getLockService().removePlayerFromLock(..., targetPlayer); // remove does not normalize first
```

Current cache API from `PlayerIdentityService`:

```java
public void preload(OfflinePlayer[] offlinePlayers)
public void remember(OfflinePlayer player)
public UUID findUuidByName(String playerName)
public String resolveStoredName(String playerName)
public String getLastKnownName(UUID uuid)
public void save()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为破坏保护与环境保护矩阵写红测合同</name>
  <files>src/test/java/ym/signLock/listener/LockListenerBreakProtectionTest.java, src/test/java/ym/signLock/listener/LockListenerProtectionMatrixTest.java</files>
  <action>新增 listener 级回归测试，直接锁定 D-01、D-02、D-03、D-04。`LockListenerBreakProtectionTest` 必须覆盖：未授权玩家不能拆受保护方块本体、不能拆他人的主锁牌/扩展牌、已授权玩家能访问但不能拆、主人和管理员绕过可以拆。`LockListenerProtectionMatrixTest` 必须覆盖：InventoryMove 的 source/destination 都不能绕过；BlockExplode 和 EntityExplode 会过滤受保护目标与受管理锁牌；Fluid 不会流入或流出受保护结构；活塞先验证 `getBlocks()` 移动列表路径，再额外加入“destination collision / attached sign side effect” 证明测试。若该风险当前可复现，就把失败断言明确写出来；若暂时不能复现，也要留下证明当前边界的测试与注释，避免计划执行时凭猜测扩大语义。不要在这个计划里修改生产代码。</action>
  <verify>
    <automated>.\gradlew.bat test --tests "ym.signLock.listener.LockListenerBreakProtectionTest" --tests "ym.signLock.listener.LockListenerProtectionMatrixTest"</automated>
  </verify>
  <done>两个 listener 新套件已存在，断言直接对应 PROT-03 / PROT-04，并且在未修复前至少对当前缺口产生红测信号或可解释的边界证明。</done>
</task>

<task type="auto">
  <name>Task 2: 为命令授权与身份缓存写红测合同</name>
  <files>src/test/java/ym/signLock/command/SignLockCommandAuthorizationTest.java, src/test/java/ym/signLock/service/PlayerIdentityServiceTest.java</files>
  <action>新增命令与身份服务回归测试，直接锁定 D-05、D-06、D-07。`SignLockCommandAuthorizationTest` 必须覆盖：`/signlock add` 和 `/signlock remove` 都以玩家当前瞄准的受管理锁牌为入口；无效目标返回明确失败；扩展牌已满返回 `NO_SPACE` 对应提示；`remove` 在目标不在授权列表时返回明确结果；大小写差异、cache-known rename、cache-unknown 文本名三类输入在 add/remove/info 上都要有断言。`PlayerIdentityServiceTest` 必须覆盖：`remember` 与 `preload` 持久化双向索引；`findUuidByName` 忽略大小写；`resolveStoredName` 在 UUID 已知时收敛到最近名称、未知时保留原文本。不要在这里引入外部名称查询，也不要写任何 Phase 3 本地化工作。</action>
  <verify>
    <automated>.\gradlew.bat test --tests "ym.signLock.command.SignLockCommandAuthorizationTest" --tests "ym.signLock.service.PlayerIdentityServiceTest"</automated>
  </verify>
  <done>命令授权与身份缓存的红测合同齐备，所有 OPS-02 / OPS-03 风险分支都有自动化断言，且测试描述足以指导后续实现而无需二次解释。</done>
</task>

</tasks>

<verification>
执行完成后，四个新测试文件都应成为 Phase 2 的执行入口；后续计划不再新增平行命名的替代测试套件。
</verification>

<success_criteria>
Phase 2 的 risky behavior changes 之前，已经存在 break protection、protection matrix、command authorization、identity cache 四组自动化回归入口，且每组都明确映射到本 phase 的 requirement 与锁定决策。
</success_criteria>

<output>
After completion, create `.planning/phases/02-authorization-protection-matrix/02-01-SUMMARY.md`
</output>
