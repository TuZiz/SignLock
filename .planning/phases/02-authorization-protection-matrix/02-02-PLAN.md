---
phase: 02-authorization-protection-matrix
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/main/java/ym/signLock/service/LockService.java
  - src/main/java/ym/signLock/listener/LockListener.java
  - src/test/java/ym/signLock/listener/LockListenerBreakProtectionTest.java
  - src/test/java/ym/signLock/listener/LockListenerProtectionMatrixTest.java
autonomous: true
requirements: [PROT-03, PROT-04]
must_haves:
  truths:
    - "未授权玩家不能破坏受保护本体或任何受管理锁牌。"
    - "已授权玩家仍然只有 access，没有 break/manage 权限。"
    - "主人与管理员绕过在 block body、main sign、more-users sign 上拥有一致的管理级破坏语义。"
    - "自动化搬运、爆炸、活塞和流体不能通过 single-container、shared target 或 managed sign 旁路现有保护。"
  artifacts:
    - path: "src/main/java/ym/signLock/service/LockService.java"
      provides: "central authorization/protection helpers for break and matrix decisions"
      contains: "canBreak or equivalent helper plus shared-target aware protection checks"
    - path: "src/main/java/ym/signLock/listener/LockListener.java"
      provides: "listener entrypoints wired to LockService matrix semantics"
      contains: "onBlockBreak and automation/environment listeners no longer rely on canAccess for destroy semantics"
  key_links:
    - from: "src/main/java/ym/signLock/listener/LockListener.java"
      to: "src/main/java/ym/signLock/service/LockService.java"
      via: "onBlockBreak permission check"
      pattern: "canBreak|canManage"
    - from: "src/main/java/ym/signLock/listener/LockListener.java"
      to: "src/main/java/ym/signLock/service/LockService.java"
      via: "automation/environment protection helpers"
      pattern: "isExplosionProtected|isProtectedAutomationTarget|findManagedSignLock"
---

<objective>
把受保护目标的破坏语义和环境保护矩阵收敛到 `LockService`，再让 `LockListener` 全部按同一套规则执行。
Purpose: 修掉当前 `BlockBreak` 误用 `canAccess` 的缺口，并把 D-03/D-04 要求的保守自动化保护变成稳定的 listener 行为。
Output: `LockService` / `LockListener` 的 Phase 2 权限矩阵实现，以及与之同步转绿的 break/matrix 回归。
</objective>

<execution_context>
@C:/Users/32633/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/32633/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-authorization-protection-matrix/02-CONTEXT.md
@.planning/phases/02-authorization-protection-matrix/02-RESEARCH.md
@.planning/phases/02-authorization-protection-matrix/02-01-PLAN.md
@.planning/phases/02-authorization-protection-matrix/02-VALIDATION.md
@src/main/java/ym/signLock/service/LockService.java
@src/main/java/ym/signLock/listener/LockListener.java
@src/test/java/ym/signLock/listener/LockListenerBreakProtectionTest.java
@src/test/java/ym/signLock/listener/LockListenerProtectionMatrixTest.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 LockService 收口破坏权限语义并修复 BlockBreak</name>
  <files>src/main/java/ym/signLock/service/LockService.java, src/main/java/ym/signLock/listener/LockListener.java, src/test/java/ym/signLock/listener/LockListenerBreakProtectionTest.java</files>
  <behavior>
    - Test 1: 未授权玩家与仅被授权玩家都会被 `BlockBreakEvent` 拦截，不能拆锁目标本体。
    - Test 2: 未授权玩家与仅被授权玩家都会被拦截，不能拆主锁牌或扩展牌。
    - Test 3: 主人与管理员绕过在锁目标本体、主锁牌、扩展牌三条路径上都能通过。
  </behavior>
  <action>基于 D-01、D-02 在 `LockService` 引入显式的 break/manage 语义收口，命名可为 `canBreak(...)` 或等价 helper，但必须让 listener 调用点读起来就是“破坏权限”而不是“访问权限”。`LockListener.onBlockBreak(...)` 对受保护本体和受管理锁牌都改走这条 helper，不再复用 `canAccess(...)`。若实现上 break 语义与 manage 等价，也要在命名和测试上把意图表达清楚。不在 listener 层重写 owner/admin/authorized 分支，不引入新的数据源；`LockService` 继续是唯一语义真源。</action>
  <verify>
    <automated>.\gradlew.bat test --tests "ym.signLock.listener.LockListenerBreakProtectionTest"</automated>
  </verify>
  <done>`LockListenerBreakProtectionTest` 转绿，且代码中已不存在“破坏路径仍用 `canAccess`”的入口。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 收紧自动化与环境保护矩阵，并显式覆盖单容器/双箱/锁牌样本</name>
  <files>src/main/java/ym/signLock/service/LockService.java, src/main/java/ym/signLock/listener/LockListener.java, src/test/java/ym/signLock/listener/LockListenerProtectionMatrixTest.java</files>
  <behavior>
    - Test 1: InventoryMove 在单容器 `InventoryHolder`、双箱 shared target、以及带受管理锁牌的目标上，只要 source 或 destination 任一端命中受保护结构就取消事件。
    - Test 2: BlockExplode / EntityExplode 不会破坏受保护本体或受管理锁牌。
    - Test 3: Fluid 不能流入或流出受保护结构。
    - Test 4: 活塞移动受保护方块时被拦截；若 destination collision / attached sign side effect 可复现，也必须被拦截。
  </behavior>
  <action>按 D-03、D-04 收紧环境保护路径，优先复用 `LockService` 的 canonical/shared target 语义，不允许 listener 分散判断。`isExplosionProtected(...)`、`isProtectedAutomationTarget(...)` 以及需要新增的辅助方法，应统一覆盖锁本体与受管理锁牌，并且在测试矩阵里显式覆盖三类样本：单容器、双箱 shared target、managed sign。对活塞风险必须先以 02-01 的测试结论为准：如果目的地副作用确实可复现，就在本计划里修复；如果测试证明当前 API 路径无法稳定复现，就保持现有 moved-block 拦截并保留证明测试，不要无证据地添加猜测性逻辑。非目标：不实现更细粒度“合法搬运白名单”，不做第三方自动化插件适配。</action>
  <verify>
    <automated>.\gradlew.bat test --tests "ym.signLock.listener.LockListenerProtectionMatrixTest"</automated>
  </verify>
  <done>`LockListenerProtectionMatrixTest` 转绿，环境保护路径与 break 路径共享同一套锁目标语义，且活塞副作用风险已通过测试证明或修复闭环处理。</done>
</task>

</tasks>

<verification>
此计划完成后，`LockListener` 的保护入口应只负责事件编排与消息反馈，真正的“谁能拆、什么算受保护对象”都必须能追溯回 `LockService`。
</verification>

<success_criteria>
PROT-03 与 PROT-04 在生产代码上闭环：未授权/已授权/主人/管理员的边界清晰，且自动化与环境事件无法绕过 single-container、shared target 与锁牌保护。
</success_criteria>

<output>
After completion, create `.planning/phases/02-authorization-protection-matrix/02-02-SUMMARY.md`
</output>
