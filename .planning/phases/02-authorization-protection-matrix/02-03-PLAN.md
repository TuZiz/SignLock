---
phase: 02-authorization-protection-matrix
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - src/main/java/ym/signLock/command/SignLockCommand.java
  - src/main/java/ym/signLock/service/PlayerIdentityService.java
  - src/main/java/ym/signLock/service/LockService.java
  - src/test/java/ym/signLock/command/SignLockCommandAuthorizationTest.java
  - src/test/java/ym/signLock/service/PlayerIdentityServiceTest.java
  - src/test/java/ym/signLock/listener/LockListenerBreakProtectionTest.java
  - src/test/java/ym/signLock/listener/LockListenerProtectionMatrixTest.java
autonomous: true
requirements: [OPS-02, OPS-03, PROT-03]
must_haves:
  truths:
    - "主人通过任意受管理锁牌执行 `/signlock add`、`/signlock remove`、`/signlock info` 都得到一致结果。"
    - "扩展牌已满、无效目标、玩家不在授权列表、大小写差异和已知 UUID 改名场景都返回正确结果。"
    - "身份缓存不会因为离线玩家、大小写差异或改名而破坏授权判断。"
    - "命令路径使用的身份归一化与核心访问/管理判断保持一致，不会出现 add/remove 非对称。"
  artifacts:
    - path: "src/main/java/ym/signLock/command/SignLockCommand.java"
      provides: "normalized add/remove/info command path"
      contains: "targeted sign resolution and explicit result handling"
    - path: "src/main/java/ym/signLock/service/PlayerIdentityService.java"
      provides: "cache-backed UUID-aware identity resolution"
      contains: "case-insensitive lookup and last-known-name convergence"
    - path: "src/test/java/ym/signLock/command/SignLockCommandAuthorizationTest.java"
      provides: "OPS-02 regression suite"
      contains: "invalid target, extension full, rename/case, owner/admin boundaries"
    - path: "src/test/java/ym/signLock/service/PlayerIdentityServiceTest.java"
      provides: "OPS-03 regression suite"
      contains: "preload/save/rename/cache-known/cache-unknown cases"
  key_links:
    - from: "src/main/java/ym/signLock/command/SignLockCommand.java"
      to: "src/main/java/ym/signLock/service/PlayerIdentityService.java"
      via: "target name normalization before add/remove/info operations"
      pattern: "resolveStoredName|findUuidByName"
    - from: "src/main/java/ym/signLock/command/SignLockCommand.java"
      to: "src/main/java/ym/signLock/service/LockService.java"
      via: "owner/admin authorization and list mutation results"
      pattern: "canManage|addPlayerToLock|removePlayerFromLock|describeLock"
---

<objective>
修复命令授权入口与身份缓存链路，让 add/remove/info 和核心权限矩阵在身份解析上完全对齐。
Purpose: 消除 `handleAdd(...)` / `handleRemove(...)` 的身份归一化非对称，并把 D-05、D-06、D-07 的结果边界固定成可回归行为。
Output: 命令与身份服务的 Phase 2 实现闭环，以及对应测试转绿。
</objective>

<execution_context>
@C:/Users/32633/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/32633/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-authorization-protection-matrix/02-CONTEXT.md
@.planning/phases/02-authorization-protection-matrix/02-RESEARCH.md
@.planning/phases/02-authorization-protection-matrix/02-01-PLAN.md
@.planning/phases/02-authorization-protection-matrix/02-02-PLAN.md
@.planning/phases/02-authorization-protection-matrix/02-VALIDATION.md
@src/main/java/ym/signLock/command/SignLockCommand.java
@src/main/java/ym/signLock/service/PlayerIdentityService.java
@src/main/java/ym/signLock/service/LockService.java
@src/test/java/ym/signLock/command/SignLockCommandAuthorizationTest.java
@src/test/java/ym/signLock/service/PlayerIdentityServiceTest.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 统一 add/remove/info 的身份归一化与结果分支</name>
  <files>src/main/java/ym/signLock/command/SignLockCommand.java, src/main/java/ym/signLock/service/PlayerIdentityService.java, src/main/java/ym/signLock/service/LockService.java, src/test/java/ym/signLock/command/SignLockCommandAuthorizationTest.java, src/test/java/ym/signLock/service/PlayerIdentityServiceTest.java</files>
  <behavior>
    - Test 1: `add` 与 `remove` 对 cache-known rename 输入都会先归一化到同一身份，再调用锁服务。
    - Test 2: cache-unknown 文本名仍允许执行授权操作，但后续一旦 UUID 已知，访问与命令判断会收敛。
    - Test 3: 无效目标、扩展牌已满、目标不在授权列表时都返回明确消息和结果。
    - Test 4: `info` 从任意受管理锁牌入口读取到同一 owner / players / extensionCount 视图。
  </behavior>
  <action>按 D-05、D-06、D-07 整理命令路径。`handleAdd(...)`、`handleRemove(...)`、`handleInfo(...)` 都继续使用“玩家当前瞄准的受管理锁牌”作为入口，不引入 GUI 或新选择机制。将身份归一化提取成对称流程，避免 `add` 用 `resolveStoredName(...)`、`remove` 却不处理的差异；需要时可在 `LockService` 补一个复用 helper，但不要把 identity 规则散落到多个命令分支。保持 cache-first、UUID-aware、case-insensitive，禁止接入 `Bukkit.getOfflinePlayer(String)` 或外部名称查询。若现有消息 API 足够，直接复用；只有在测试证明需要新分支时才扩展 `SignLockConfig`。</action>
  <verify>
    <automated>.\gradlew.bat test --tests "ym.signLock.command.SignLockCommandAuthorizationTest" --tests "ym.signLock.service.PlayerIdentityServiceTest"</automated>
  </verify>
  <done>OPS-02 / OPS-03 套件转绿，命令身份归一化不再存在 add/remove 非对称，且 info/add/remove 对主锁牌、扩展牌、单箱、双箱入口保持一致。</done>
</task>

<task type="auto">
  <name>Task 2: 跑 Phase 2 全量回归并只修命令/身份链收尾缺口</name>
  <files>src/main/java/ym/signLock/command/SignLockCommand.java, src/main/java/ym/signLock/service/PlayerIdentityService.java, src/main/java/ym/signLock/service/LockService.java, src/test/java/ym/signLock/command/SignLockCommandAuthorizationTest.java, src/test/java/ym/signLock/service/PlayerIdentityServiceTest.java, src/test/java/ym/signLock/listener/LockListenerBreakProtectionTest.java, src/test/java/ym/signLock/listener/LockListenerProtectionMatrixTest.java</files>
  <action>在 02-02 已完成破坏/环境保护矩阵后，执行 Phase 2 相关的新旧回归，并只修命令与身份链实现过程中暴露的本 phase 收尾问题。范围限定为授权管理、身份解析与由此直接触发的 `LockService` 身份归一化辅助逻辑；不再修改 `LockListener.java`，也不再承担 PROT-04 listener 侧修复职责。必须回归现有 Phase 1 套件，确认 shared target 和 sign-edit 语义没有被连带破坏。非目标：不处理 Phase 3 的资源乱码、发布说明、reload smoke、兼容性文档。若发现超出命令/身份链范围的问题，只在总结里记录，不顺手扩 scope。</action>
  <verify>
    <automated>.\gradlew.bat test --tests "ym.signLock.listener.*" --tests "ym.signLock.command.SignLockCommandAuthorizationTest" --tests "ym.signLock.service.*"</automated>
  </verify>
  <done>Phase 1 既有测试与 Phase 2 新增测试共同通过，且剩余修改都能清晰追溯到 PROT-03 / PROT-04 / OPS-02 / OPS-03，而不是 Phase 3 发布整理。</done>
</task>

</tasks>

<verification>
全量回归必须至少覆盖四个新增入口：`LockListenerBreakProtectionTest`、`LockListenerProtectionMatrixTest`、`SignLockCommandAuthorizationTest`、`PlayerIdentityServiceTest`，并回归 Phase 1 的 shared-target listener/service 套件。
</verification>

<success_criteria>
OPS-02 与 OPS-03 收口完成，命令入口和身份缓存链路与核心权限矩阵一致；执行结束后，Phase 2 只剩 UAT/verification，而没有明显的未自动化边界。
</success_criteria>

<output>
After completion, create `.planning/phases/02-authorization-protection-matrix/02-03-SUMMARY.md`
</output>
