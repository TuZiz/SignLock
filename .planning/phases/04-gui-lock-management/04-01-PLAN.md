---
phase: 04-gui-lock-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/ym/signLock/gui/LockManagementSession.java
  - src/main/java/ym/signLock/gui/LockSummaryView.java
  - src/main/java/ym/signLock/gui/LockSummaryTarget.java
  - src/test/java/ym/signLock/gui/LockManagementGuiViewTest.java
  - src/test/java/ym/signLock/listener/LockListenerGuiEntryTest.java
autonomous: true
requirements:
  - GUI-01
  - GUI-02
must_haves:
  truths:
    - "执行器有可回归的 GUI 入口测试基线，能证明 owner 普通右键与潜行右键会走不同分支。"
    - "GUI 摘要模型能稳定表达 owner、授权玩家、扩展牌数量和 canonical target 摘要。"
    - "后续 GUI 渲染与动作层不需要重新发明锁语义或目标解析规则。"
  artifacts:
    - path: "src/main/java/ym/signLock/gui/LockManagementSession.java"
      provides: "GUI 会话键与 canonical target 绑定"
    - path: "src/main/java/ym/signLock/gui/LockSummaryView.java"
      provides: "单页摘要视图模型"
    - path: "src/test/java/ym/signLock/gui/LockManagementGuiViewTest.java"
      provides: "GUI-02 摘要回归"
    - path: "src/test/java/ym/signLock/listener/LockListenerGuiEntryTest.java"
      provides: "GUI-01 入口回归"
  key_links:
    - from: "src/main/java/ym/signLock/gui/LockSummaryView.java"
      to: "src/main/java/ym/signLock/service/LockService.java"
      via: "describeLock + canonical target summary assembly"
      pattern: "describeLock|targetBlock"
    - from: "src/test/java/ym/signLock/listener/LockListenerGuiEntryTest.java"
      to: "src/main/java/ym/signLock/listener/LockListener.java"
      via: "owner right-click and sneak-right-click regression coverage"
      pattern: "RIGHT_CLICK_BLOCK|openSign"
---

<objective>
为 Phase 4 建立 GUI 的执行基线：先把会话契约、摘要视图模型和 Wave 0 测试脚手架写清楚，让后续入口接线与动作层都能直接落实现。

Purpose: 先固化接口和回归基线，避免后两份计划在 GUI 结构、target 语义或验证口径上继续发散。
Output: GUI 会话/摘要模型代码骨架 + GUI 入口/摘要测试类。
</objective>

<execution_context>
@C:/Users/32633/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/32633/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-gui-lock-management/04-CONTEXT.md
@.planning/phases/04-gui-lock-management/04-RESEARCH.md
@.planning/phases/04-gui-lock-management/04-VALIDATION.md
@src/main/java/ym/signLock/service/LockService.java
@src/main/java/ym/signLock/listener/LockListener.java

<interfaces>
From src/main/java/ym/signLock/service/LockService.java:
```java
public LockInfo findManagedSignLock(Block block)
public LockDetails describeLock(Block signBlock)
public boolean canManage(LockInfo lock, Player player)
public record LockInfo(Block signBlock, Block targetBlock, String owner, Set<String> allowedPlayers, LockType type) {}
public record LockDetails(String owner, List<String> allowedPlayers, int extensionCount) {}
```

From src/main/java/ym/signLock/listener/LockListener.java:
```java
private boolean tryOpenOwnedSignEditor(PlayerInteractEvent event, Player player, Block clicked)
```

Plan contract:
```java
record LockManagementSession(UUID worldId, int x, int y, int z, Location signLocation) {}
record LockSummaryTarget(String blockType, String worldName, int x, int y, int z) {}
record LockSummaryView(String owner, List<String> allowedPlayers, int extensionCount, LockSummaryTarget target) {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 GUI 入口与摘要的 Wave 0 测试脚手架</name>
  <files>src/test/java/ym/signLock/listener/LockListenerGuiEntryTest.java, src/test/java/ym/signLock/gui/LockManagementGuiViewTest.java</files>
  <behavior>
    - Test 1: owner 普通右键受管理锁牌时，事件被取消并走 GUI 打开分支，满足 D-01 / D-07。
    - Test 2: owner 潜行右键相同锁牌时，保留原生 sign edit fallback，不误开 GUI，满足 D-08。
    - Test 3: 摘要视图基于 canonical target 输出 owner、allowedPlayers、extensionCount 与目标摘要，满足 D-03 / D-04。
  </behavior>
  <action>新增 `LockListenerGuiEntryTest` 与 `LockManagementGuiViewTest`，先把 GUI-01 和 GUI-02 的验证口径固定下来。测试中明确约束普通右键开 GUI、潜行右键保留 `Player#openSign(...)`、非 owner 不开管理 GUI；摘要视图测试必须复用 `LockService.describeLock(...)` 与 `LockInfo.targetBlock()` 产物，不允许构造第二套锁数据模型。若缺少 GUI 类，先以最小占位接口编译通过，但不要在这一任务里接入真实 chest inventory。</action>
  <verify>
    <automated>.\gradlew.bat test --tests ym.signLock.listener.LockListenerGuiEntryTest --tests ym.signLock.gui.LockManagementGuiViewTest</automated>
  </verify>
  <done>两个测试类存在并清楚表达入口分流与摘要契约，失败点直接对应 GUI-01 / GUI-02。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 编写 GUI 会话键与摘要视图模型契约</name>
  <files>src/main/java/ym/signLock/gui/LockManagementSession.java, src/main/java/ym/signLock/gui/LockSummaryView.java, src/main/java/ym/signLock/gui/LockSummaryTarget.java</files>
  <behavior>
    - Test 1: 同一把锁从不同 managed sign 进入时，会话仍绑定同一个 canonical target。
    - Test 2: 摘要模型可承载 GUI 首屏展示所需的 owner、玩家列表、扩展牌数和目标摘要。
  </behavior>
  <action>创建 `ym.signLock.gui` 包下的会话与视图模型，只做 Phase 4 执行需要的最小契约。`LockManagementSession` 必须以 world UUID + xyz + sign location 绑定 canonical target，避免后续双箱/扩展牌漂移；`LockSummaryView` / `LockSummaryTarget` 负责把 `LockService` 结果整理成单页摘要模型，满足 D-03 / D-04。不要加入分页、批量操作、复杂布局状态，避免触发 deferred ideas。</action>
  <verify>
    <automated>.\gradlew.bat test --tests ym.signLock.listener.LockListenerGuiEntryTest --tests ym.signLock.gui.LockManagementGuiViewTest</automated>
  </verify>
  <done>GUI 基础契约存在且通过新测试，后续计划可以直接围绕这些类型实现真实 GUI。</done>
</task>

</tasks>

<verification>
先跑新增入口/视图测试；如果测试依赖最小占位实现，允许在本计划内只补足契约代码，不提前接线 chest GUI。
</verification>

<success_criteria>
执行完成后，Phase 4 已有明确的 GUI 入口回归和单页摘要模型契约，且这些契约显式遵守 D-01、D-03、D-04、D-07、D-08。
</success_criteria>

<output>
After completion, create `.planning/phases/04-gui-lock-management/04-01-SUMMARY.md`
</output>
