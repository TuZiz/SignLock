---
phase: 04-gui-lock-management
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/main/java/ym/signLock/gui/LockManagementGui.java
  - src/main/java/ym/signLock/gui/LockManagementGuiHolder.java
  - src/main/java/ym/signLock/gui/LockManagementGuiService.java
  - src/main/java/ym/signLock/listener/LockGuiListener.java
  - src/main/java/ym/signLock/listener/LockListener.java
  - src/test/java/ym/signLock/gui/LockGuiListenerClickSafetyTest.java
  - src/test/java/ym/signLock/gui/LockManagementGuiViewTest.java
  - src/test/java/ym/signLock/listener/LockListenerGuiEntryTest.java
autonomous: true
requirements:
  - GUI-01
  - GUI-02
must_haves:
  truths:
    - "锁主人普通右键 managed sign 会稳定打开单页 chest GUI。"
    - "潜行右键仍直接进入原生 sign 编辑，不出现 GUI 闪烁或抢占。"
    - "GUI 渲染的摘要内容来自前一计划的视图模型，且点击/拖拽不会让物品进入 GUI。"
  artifacts:
    - path: "src/main/java/ym/signLock/gui/LockManagementGuiService.java"
      provides: "GUI 打开、刷新和 next-tick 切换编排"
    - path: "src/main/java/ym/signLock/gui/LockManagementGuiHolder.java"
      provides: "自定义 chest GUI holder 与 slot 路由"
    - path: "src/main/java/ym/signLock/listener/LockGuiListener.java"
      provides: "GUI click/drag safety 监听"
    - path: "src/main/java/ym/signLock/listener/LockListener.java"
      provides: "managed sign 入口切换为普通右键开 GUI、潜行右键保留编辑"
  key_links:
    - from: "src/main/java/ym/signLock/listener/LockListener.java"
      to: "src/main/java/ym/signLock/gui/LockManagementGuiService.java"
      via: "tryOpenOwnedSignEditor entry replacement"
      pattern: "openFor|player.isSneaking"
    - from: "src/main/java/ym/signLock/listener/LockGuiListener.java"
      to: "src/main/java/ym/signLock/gui/LockManagementGuiHolder.java"
      via: "InventoryClickEvent and InventoryDragEvent routing"
      pattern: "runTask|setCancelled"
---

<objective>
把 Phase 4 的 GUI 主交互真正接上线：owner 普通右键 managed sign 打开单页 chest GUI，潜行右键保留原生 sign 编辑，并把 inventory click/drag 的所有跳转收口到 next tick。

Purpose: 完成 GUI-01 的真实入口替换与 GUI-02 的真实渲染载体，同时提前锁死 Spigot inventory 事件安全边界。
Output: chest GUI 渲染/持有者/监听器实现 + 入口接线 + click safety 回归。
</objective>

<execution_context>
@C:/Users/32633/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/32633/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-gui-lock-management/04-CONTEXT.md
@.planning/phases/04-gui-lock-management/04-RESEARCH.md
@.planning/phases/04-gui-lock-management/04-VALIDATION.md
@.planning/phases/04-gui-lock-management/04-01-PLAN.md
@src/main/java/ym/signLock/listener/LockListener.java
@src/main/java/ym/signLock/service/LockService.java
@src/main/java/ym/signLock/config/SignLockConfig.java

<interfaces>
From src/main/java/ym/signLock/config/SignLockConfig.java:
```java
public String addOnlyOwnerMessage()
public String addInvalidSignMessage()
public String signEditDeniedMessage()
public String infoOwnerMessage(String owner)
public String infoPlayersMessage(String players)
public String infoNoPlayersMessage()
public String infoExtensionsMessage(int count)
```

From 04-01 contracts:
```java
record LockManagementSession(UUID worldId, int x, int y, int z, Location signLocation) {}
record LockSummaryView(String owner, List<String> allowedPlayers, int extensionCount, LockSummaryTarget target) {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 GUI 入口替换与摘要渲染</name>
  <files>src/main/java/ym/signLock/gui/LockManagementGui.java, src/main/java/ym/signLock/gui/LockManagementGuiHolder.java, src/main/java/ym/signLock/gui/LockManagementGuiService.java, src/main/java/ym/signLock/listener/LockListener.java, src/test/java/ym/signLock/gui/LockManagementGuiViewTest.java, src/test/java/ym/signLock/listener/LockListenerGuiEntryTest.java</files>
  <behavior>
    - Test 1: owner 普通右键受管理 sign 时打开单页 summary-first chest GUI，满足 D-01 / D-03 / D-07。
    - Test 2: owner 潜行右键仍调用原生 sign 编辑，不误触 GUI，满足 D-08。
    - Test 3: GUI 首屏展示 owner、授权玩家、扩展牌数量和目标摘要，满足 D-04。
  </behavior>
  <action>实现 repo 内轻量 chest GUI，不引入第三方 GUI 框架，满足 research 的 primary recommendation。`LockListener` 中将现有 `tryOpenOwnedSignEditor(...)` 收口为“普通右键 -> `LockManagementGuiService.openFor(...)`；潜行右键 -> `player.openSign(sign)`”；GUI 必须保持单页 summary-first，先渲染摘要，再留出 add/remove/refresh/cancel 操作槽位。布局和图标可轻量，但不要加分页、批量按钮、复杂视觉层，避免违反 D-05 和 deferred ideas。</action>
  <verify>
    <automated>.\gradlew.bat test --tests ym.signLock.listener.LockListenerGuiEntryTest --tests ym.signLock.gui.LockManagementGuiViewTest</automated>
  </verify>
  <done>普通右键 GUI 入口与潜行回退都由自动测试覆盖，且 GUI 渲染内容满足 GUI-02 摘要要求。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 收口 click/drag 安全与 next-tick 界面切换</name>
  <files>src/main/java/ym/signLock/listener/LockGuiListener.java, src/main/java/ym/signLock/gui/LockManagementGuiService.java, src/test/java/ym/signLock/gui/LockGuiListenerClickSafetyTest.java</files>
  <behavior>
    - Test 1: GUI click、shift-click、number key、drag 都会被取消，物品不能进入 GUI。
    - Test 2: 任意 open/close/refresh/action follow-up 都通过 next-tick 调度，不在 inventory 事件内直接切换界面。
  </behavior>
  <action>新增 `LockGuiListener` 监听 `InventoryClickEvent` / `InventoryDragEvent`，只对自定义 `LockManagementGuiHolder` 生效。监听器中统一 `event.setCancelled(true)`，仅解析动作并通过 Bukkit 主线程调度器 next tick 调用 `LockManagementGuiService` 执行刷新、关闭或后续输入桥接。这里必须在动作层之前解决 click safety，直接落实 locked guidance“Inventory click/drag transitions must defer open/close/refresh to next tick”。不要把业务写回放进监听器；监听器只做取消、识别、调度。</action>
  <verify>
    <automated>.\gradlew.bat test --tests ym.signLock.gui.LockGuiListenerClickSafetyTest --tests ym.signLock.listener.LockListenerGuiEntryTest --tests ym.signLock.gui.LockManagementGuiViewTest</automated>
  </verify>
  <done>GUI 点击安全边界由单独测试锁定，后续动作计划可以在不碰 inventory 线程风险的前提下继续扩展。</done>
</task>

</tasks>

<verification>
先跑 GUI 入口与渲染测试，再跑 click safety 测试；若 MockBukkit 难以断言调度细节，至少要断言监听器不直接调用 inventory transition 且存在 next-tick 调度。
</verification>

<success_criteria>
执行完成后，Phase 4 已拥有真实可开的 chest GUI 主入口、稳定的摘要渲染，以及 inventory 事件安全护栏。
</success_criteria>

<output>
After completion, create `.planning/phases/04-gui-lock-management/04-02-SUMMARY.md`
</output>
