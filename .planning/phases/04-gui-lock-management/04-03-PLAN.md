---
phase: 04-gui-lock-management
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/main/java/ym/signLock/gui/LockManagementGuiActionService.java
  - src/main/java/ym/signLock/gui/LockManagementPendingInputStore.java
  - src/main/java/ym/signLock/listener/LockGuiChatInputListener.java
  - src/main/java/ym/signLock/gui/LockManagementGuiService.java
  - src/main/java/ym/signLock/command/SignLockCommand.java
  - src/test/java/ym/signLock/gui/LockManagementGuiActionTest.java
  - src/test/java/ym/signLock/listener/LockGuiChatInputTest.java
  - src/test/java/ym/signLock/listener/LockListenerGuiEntryTest.java
  - src/test/java/ym/signLock/gui/LockGuiListenerClickSafetyTest.java
autonomous: true
requirements:
  - GUI-03
must_haves:
  truths:
    - "锁主人可以在 GUI 中执行单人 add/remove，结果与 `/bl add`、`/bl remove` 一致。"
    - "新增授权的输入桥接不会把 Bukkit 状态操作留在异步聊天线程中。"
    - "Phase 4 closeout 会带着入口、摘要、安全和动作回归一起收口。"
  artifacts:
    - path: "src/main/java/ym/signLock/gui/LockManagementGuiActionService.java"
      provides: "GUI 单人 add/remove 行为编排"
    - path: "src/main/java/ym/signLock/gui/LockManagementPendingInputStore.java"
      provides: "待输入会话状态"
    - path: "src/main/java/ym/signLock/listener/LockGuiChatInputListener.java"
      provides: "guided chat input async capture + sync handoff"
    - path: "src/test/java/ym/signLock/gui/LockManagementGuiActionTest.java"
      provides: "GUI-03 行为回归"
    - path: "src/test/java/ym/signLock/listener/LockGuiChatInputTest.java"
      provides: "聊天输入桥接回归"
  key_links:
    - from: "src/main/java/ym/signLock/gui/LockManagementGuiActionService.java"
      to: "src/main/java/ym/signLock/service/LockService.java"
      via: "canManage + addPlayerToLock + removePlayerFromLock"
      pattern: "canManage|addPlayerToLock|removePlayerFromLock"
    - from: "src/main/java/ym/signLock/listener/LockGuiChatInputListener.java"
      to: "src/main/java/ym/signLock/service/PlayerIdentityService.java"
      via: "resolveStoredName + sync writeback"
      pattern: "resolveStoredName|runTask"
---

<objective>
完成 Phase 4 最后一层可用功能：在 GUI 中做单人 add/remove、以聊天输入桥接补齐 add-player、并用整套回归把 GUI-03 与前两波行为一起收口。

Purpose: 把 GUI 从“可打开的摘要面板”推进到“可安全管理授权”的完整 owner 工作流，同时不破坏现有命令与 sign-backed 数据格式。
Output: GUI 动作服务、聊天输入桥接、回归测试和 Phase 4 closeout 验证。
</objective>

<execution_context>
@C:/Users/32633/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/32633/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-gui-lock-management/04-CONTEXT.md
@.planning/phases/04-gui-lock-management/04-RESEARCH.md
@.planning/phases/04-gui-lock-management/04-VALIDATION.md
@.planning/phases/04-gui-lock-management/04-02-PLAN.md
@src/main/java/ym/signLock/service/LockService.java
@src/main/java/ym/signLock/service/PlayerIdentityService.java
@src/main/java/ym/signLock/command/SignLockCommand.java
@src/main/java/ym/signLock/config/SignLockConfig.java

<interfaces>
From src/main/java/ym/signLock/service/LockService.java:
```java
public AddPlayerResult addPlayerToLock(Sign preferredSign, LockInfo lock, String playerName)
public RemovePlayerResult removePlayerFromLock(Sign preferredSign, LockInfo lock, String playerName)
public boolean canManage(LockInfo lock, Player player)
public enum AddPlayerResult { ADDED, ADDED_WITH_EXTENSION, ALREADY_AUTHORIZED, NO_SPACE }
public enum RemovePlayerResult { REMOVED, NOT_FOUND, OWNER_DENIED }
```

From src/main/java/ym/signLock/service/PlayerIdentityService.java:
```java
public void remember(OfflinePlayer player)
public String resolveStoredName(String playerName)
public void save()
```

From src/main/java/ym/signLock/command/SignLockCommand.java:
```java
private String normalizeTargetPlayer(Player actor, String targetPlayer)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 GUI 单人 add/remove 动作服务</name>
  <files>src/main/java/ym/signLock/gui/LockManagementGuiActionService.java, src/main/java/ym/signLock/gui/LockManagementGuiService.java, src/test/java/ym/signLock/gui/LockManagementGuiActionTest.java</files>
  <behavior>
    - Test 1: remove 动作复用 `LockService.removePlayerFromLock(...)`，并正确区分 REMOVED / NOT_FOUND / OWNER_DENIED，满足 D-06。
    - Test 2: add 动作不会直接改 sign 文本，而是走统一 service 结果语义，必要时支持 ADDED_WITH_EXTENSION。
    - Test 3: GUI action 只支持单人 add/remove，不出现 batch 入口，满足 D-05。
  </behavior>
  <action>实现 GUI 动作服务，把 GUI 槽位动作映射到 `LockService.canManage(...)`、`addPlayerToLock(...)`、`removePlayerFromLock(...)`，并将结果消息对齐现有 `SignLockConfig` / `SignLockCommand` 语义。remove 可以采用“点击玩家槽位即移除”；add 则只进入待输入状态，不在本任务里写聊天监听。严禁直接修改 sign 行内容或维护第二套授权列表，严格落实 D-06。不要补批量操作、搜索、分页或更重 UI 功能。</action>
  <verify>
    <automated>.\gradlew.bat test --tests ym.signLock.gui.LockManagementGuiActionTest --tests ym.signLock.gui.LockGuiListenerClickSafetyTest</automated>
  </verify>
  <done>GUI 已具备单人 add/remove 的业务动作层，且 service 复用与 scope guard 都被测试锁定。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 接入聊天输入桥接并完成 Phase 4 回归收口</name>
  <files>src/main/java/ym/signLock/gui/LockManagementPendingInputStore.java, src/main/java/ym/signLock/listener/LockGuiChatInputListener.java, src/main/java/ym/signLock/gui/LockManagementGuiService.java, src/main/java/ym/signLock/command/SignLockCommand.java, src/test/java/ym/signLock/listener/LockGuiChatInputTest.java, src/test/java/ym/signLock/gui/LockManagementGuiActionTest.java, src/test/java/ym/signLock/listener/LockListenerGuiEntryTest.java, src/test/java/ym/signLock/gui/LockGuiListenerClickSafetyTest.java</files>
  <behavior>
    - Test 1: add-player 按钮触发 guided chat input，异步聊天事件只捕获文本并切回同步写回锁数据。
    - Test 2: 输入名称会经过与命令相同的 identity normalization 语义，满足 D-06。
    - Test 3: Phase 4 相关测试一起通过，证明 GUI-01 到 GUI-03 已闭环。
  </behavior>
  <action>新增轻量待输入存储和聊天监听，只服务于“单人 add”桥接。聊天事件中只取消广播、读取文本、查找待输入会话；真正的 `resolveStoredName(...)`、`LockService.addPlayerToLock(...)`、GUI reopen/refresh 必须切回同步调度，落实 research 里的 async chat pitfall。可以复用或抽取 `SignLockCommand.normalizeTargetPlayer(...)` 的逻辑，但不要让命令兼容路径失效，满足 D-02。最后按 04-VALIDATION 的采样策略跑完 Phase 4 全量相关测试并补 manual verification 说明到 SUMMARY。</action>
  <verify>
    <automated>.\gradlew.bat test --tests ym.signLock.listener.LockListenerGuiEntryTest --tests ym.signLock.gui.LockManagementGuiViewTest --tests ym.signLock.gui.LockGuiListenerClickSafetyTest --tests ym.signLock.gui.LockManagementGuiActionTest --tests ym.signLock.listener.LockGuiChatInputTest</automated>
  </verify>
  <done>GUI 单人 add/remove 工作流闭环，聊天输入桥接线程安全，且 Phase 4 所有关联自动化测试一起为绿。</done>
</task>

</tasks>

<verification>
自动化完成后，再按 `04-VALIDATION.md` 做一次真实客户端手测：普通右键开 GUI、潜行右键开原生编辑、add-player 成功/失败后 GUI 都能稳定返回。
</verification>

<success_criteria>
执行完成后，锁主人可以在 GUI 中完成单人授权管理，命令兼容路径仍然可用，且 Phase 4 相关回归全部闭环为绿。
</success_criteria>

<output>
After completion, create `.planning/phases/04-gui-lock-management/04-03-SUMMARY.md`
</output>
