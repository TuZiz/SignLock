---
phase: 01-lock-target-correctness
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/main/java/ym/signLock/listener/LockListener.java
  - src/test/java/ym/signLock/listener/LockListenerInteractTest.java
  - src/test/java/ym/signLock/listener/LockListenerInventoryOpenTest.java
autonomous: true
requirements:
  - PROT-01
  - PROT-02
must_haves:
  truths:
    - "未授权玩家右键共享锁目标时，无论点到双箱哪一半，都会被稳定拦截。"
    - "未授权玩家通过 `InventoryHolder` 打开受保护容器时，会收到容器拦截提示而不是绕过共享目标语义。"
    - "交互拦截与开箱拦截复用相同的 canonical target 判定，不会出现一个入口放行另一个入口拦截。"
  artifacts:
    - path: "src/main/java/ym/signLock/listener/LockListener.java"
      provides: "右键、开箱、自动化入口的一致拦截"
      contains: "onPlayerInteract"
    - path: "src/test/java/ym/signLock/listener/LockListenerInteractTest.java"
      provides: "PROT-01 回归测试"
      contains: "RIGHT_CLICK_BLOCK"
    - path: "src/test/java/ym/signLock/listener/LockListenerInventoryOpenTest.java"
      provides: "PROT-02 回归测试"
      contains: "InventoryOpenEvent"
  key_links:
    - from: "src/main/java/ym/signLock/listener/LockListener.java"
      to: "src/main/java/ym/signLock/service/LockService.java"
      via: "findLock / resolveInventoryBlock / canAccess"
      pattern: "lockService\\.(findLock|resolveInventoryBlock|canAccess)"
    - from: "src/test/java/ym/signLock/listener/LockListenerInventoryOpenTest.java"
      to: "src/main/java/ym/signLock/listener/LockListener.java"
      via: "InventoryHolder shared-target regression"
      pattern: "lockedContainerMessage"
---

<objective>
让玩家交互与容器访问两个核心入口复用 Plan 01 产出的 canonical target 语义，修掉 PROT-01 / PROT-02 的分叉风险。

Purpose: 共享目标归一只在服务层成立还不够，监听器必须全部接上这条语义链，否则双箱仍会从不同入口出现绕过或误判。
Output: 两组 listener 回归测试，以及完成一致性修复后的 `LockListener` 交互/开箱逻辑。
</objective>

<execution_context>
@C:/Users/32633/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/32633/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-lock-target-correctness/01-CONTEXT.md
@.planning/phases/01-lock-target-correctness/01-RESEARCH.md
@src/main/java/ym/signLock/listener/LockListener.java
@src/main/java/ym/signLock/service/LockService.java
@build.gradle

<interfaces>
From `src/main/java/ym/signLock/listener/LockListener.java`:
```java
public void onPlayerInteract(PlayerInteractEvent event);
public void onInventoryOpen(InventoryOpenEvent event);
public void onInventoryMove(InventoryMoveItemEvent event);
```

From `src/main/java/ym/signLock/service/LockService.java`:
```java
public Block resolveInventoryBlock(InventoryHolder holder);
public LockInfo findLock(Block block);
public boolean canAccess(LockInfo lock, Player player);
public boolean isProtectedAutomationTarget(Block block);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为右键与开箱路径补齐共享目标拦截回归测试</name>
  <read_first>
    - src/main/java/ym/signLock/listener/LockListener.java
    - src/main/java/ym/signLock/service/LockService.java
    - .planning/phases/01-lock-target-correctness/01-VALIDATION.md
    - src/test/java/ym/signLock/service/LockServiceCanonicalTargetTest.java
  </read_first>
  <files>src/test/java/ym/signLock/listener/LockListenerInteractTest.java, src/test/java/ym/signLock/listener/LockListenerInventoryOpenTest.java</files>
  <action>创建 `LockListenerInteractTest` 与 `LockListenerInventoryOpenTest`。`LockListenerInteractTest` 覆盖 PROT-01：未授权玩家从双箱任一半边右键都被 `lockedUseMessage()` 拦截，已授权玩家正常通过；直接点击锁牌进入编辑器的 owner-only 逻辑不能回归。`LockListenerInventoryOpenTest` 覆盖 PROT-02：`Container` 与 `DoubleChest` 两种 holder 路径都会先解析共享目标，再统一调用 `findLock` / `canAccess`，未授权时发送 `lockedContainerMessage()`。测试中复用 Plan 01 的 canonical target 设定，不允许各自手写另一套双箱判断。</action>
  <acceptance_criteria>
    - `rg -n "class LockListenerInteractTest|class LockListenerInventoryOpenTest" src/test/java/ym/signLock/listener` 能命中两个测试类。
    - 测试断言同时覆盖“点击任一半边”和“DoubleChest holder 打开容器”两个共享目标场景。
    - `./gradlew.bat test --tests "ym.signLock.listener.LockListenerInteractTest" --tests "ym.signLock.listener.LockListenerInventoryOpenTest"` 能运行到 listener 行为断言阶段。
  </acceptance_criteria>
  <verify>
    <automated>./gradlew.bat test --tests "ym.signLock.listener.LockListenerInteractTest" --tests "ym.signLock.listener.LockListenerInventoryOpenTest"</automated>
  </verify>
  <done>PROT-01 / PROT-02 的 listener 红测已经建立，后续改动会直接暴露交互与开箱路径的不一致。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 收口 LockListener 的交互、开箱与自动化入口到同一锁语义</name>
  <read_first>
    - src/main/java/ym/signLock/listener/LockListener.java
    - src/main/java/ym/signLock/service/LockService.java
    - src/test/java/ym/signLock/listener/LockListenerInteractTest.java
    - src/test/java/ym/signLock/listener/LockListenerInventoryOpenTest.java
    - .planning/phases/01-lock-target-correctness/01-CONTEXT.md
  </read_first>
  <files>src/main/java/ym/signLock/listener/LockListener.java</files>
  <behavior>
    - Test 1: 未授权玩家右键双箱任意半边时稳定收到 `locked-use` 提示，已授权玩家不受影响。
    - Test 2: `InventoryOpenEvent` 中无论 holder 是 `Container` 还是 `DoubleChest`，未授权玩家都收到 `locked-container` 提示。
    - Test 3: `InventoryMoveItemEvent` 对共享目标的自动化保护仍然走同一 target 解析，不引入新的绕过路径。
  </behavior>
  <action>在 `LockListener` 中按 D-07 保留 `locked-use` 与 `locked-container` 的双提示语义，但把 `onPlayerInteract`、`onInventoryOpen`、`onInventoryMove` 全部接到 Plan 01 的 canonical target 解析链上。右键路径只允许“直接上锁”和“主人打开牌子编辑器”作为特例，除此之外统一以 `lockService.findLock(...)` + `canAccess(...)` 判定；开箱与自动化路径则统一走 `resolveInventoryBlock(...)` 后再判定。不要新增 listener 本地 chest-half 判断，不要把 `DoubleChest#getLocation()` 当作持久化 key，也不要改变 owner 打开编辑器的既有行为。</action>
  <acceptance_criteria>
    - `LockListenerInteractTest` 与 `LockListenerInventoryOpenTest` 通过。
    - `rg -n "lockedUseMessage|lockedContainerMessage|resolveInventoryBlock|findLock" src/main/java/ym/signLock/listener/LockListener.java` 能看出交互和开箱都复用同一服务层语义。
    - `./gradlew.bat test --tests "ym.signLock.listener.*"` 通过，且没有因为 listener 自己重新实现双箱逻辑而产生重复判断代码。
  </acceptance_criteria>
  <verify>
    <automated>./gradlew.bat test --tests "ym.signLock.listener.LockListenerInteractTest" --tests "ym.signLock.listener.LockListenerInventoryOpenTest"</automated>
  </verify>
  <done>PROT-01 / PROT-02 在交互和容器入口上实现同一共享目标语义，双箱不再因为入口不同出现绕过或误拦。</done>
</task>

</tasks>

<verification>
先让两组 listener 红测落盘，再修改 `LockListener` 转绿。任务完成后运行 `./gradlew.bat test --tests "ym.signLock.listener.*"`，确认 listener 路径在 Wave 2 内部闭环。
</verification>

<success_criteria>
- 未授权玩家无法通过交互或 InventoryHolder 差异绕过共享目标保护。
- `LockListener` 不再包含与 `LockService` 冲突的双箱局部语义。
- `locked-use` 与 `locked-container` 保持分开，符合 D-07。
</success_criteria>

<output>
After completion, create `.planning/phases/01-lock-target-correctness/01-02-SUMMARY.md`
</output>
