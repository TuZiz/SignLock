---
phase: 01-lock-target-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build.gradle
  - src/main/java/ym/signLock/service/LockService.java
  - src/test/java/ym/signLock/service/LockServiceCanonicalTargetTest.java
  - src/test/java/ym/signLock/service/LockServiceExtensionTest.java
autonomous: true
requirements:
  - LOCK-01
  - LOCK-02
  - LOCK-03
must_haves:
  truths:
    - "支持的单容器、双箱和共享容器在扫描主锁与扩展牌前都会先归一到同一个 canonical target。"
    - "双箱任意半边都能解析到同一把主锁，且扩展牌授权对整个共享目标生效。"
    - "命令与后续监听入口读取到的 `LockInfo.targetBlock()` 始终是稳定的共享目标，而不是点击半边。"
  artifacts:
    - path: "src/main/java/ym/signLock/service/LockService.java"
      provides: "canonical target 解析、共享目标扫描、扩展牌写入/读取"
      contains: "LockInfo"
    - path: "src/test/java/ym/signLock/service/LockServiceCanonicalTargetTest.java"
      provides: "LOCK-01 / LOCK-03 回归测试"
      contains: "double chest"
    - path: "src/test/java/ym/signLock/service/LockServiceExtensionTest.java"
      provides: "LOCK-02 回归测试"
      contains: "addPlayerToLock"
    - path: "build.gradle"
      provides: "JUnit + MockBukkit + Mockito 测试引导"
      contains: "useJUnitPlatform"
  key_links:
    - from: "src/main/java/ym/signLock/service/LockService.java"
      to: "src/test/java/ym/signLock/service/LockServiceCanonicalTargetTest.java"
      via: "canonical target regression coverage"
      pattern: "findPlacementTarget|resolveDirectLockTarget|findLock"
    - from: "src/main/java/ym/signLock/service/LockService.java"
      to: "src/test/java/ym/signLock/service/LockServiceExtensionTest.java"
      via: "extension sign shared-target coverage"
      pattern: "collectManagedSigns|addPlayerToLock|canCreateMoreUsersSign"
---

<objective>
收口 Phase 01 的唯一锁语义源，让 `LockService` 成为单箱/双箱/共享容器的 canonical target 解析中心，并补齐最小自动化回归底座。

Purpose: 先修正共享目标归一，再让后续交互、开箱和贴牌都复用同一语义，避免在监听器里做分叉修补。
Output: 可运行的测试基础设施、两组服务层回归测试、以及完成归一化后的 `LockService`。
</objective>

<execution_context>
@C:/Users/32633/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/32633/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-lock-target-correctness/01-CONTEXT.md
@.planning/phases/01-lock-target-correctness/01-RESEARCH.md
@.planning/phases/01-lock-target-correctness/01-VALIDATION.md
@build.gradle
@src/main/java/ym/signLock/service/LockService.java
@src/main/java/ym/signLock/service/PlayerIdentityService.java
@src/main/java/ym/signLock/command/SignLockCommand.java

<interfaces>
From `src/main/java/ym/signLock/service/LockService.java`:
```java
public Block findPlacementTarget(Block signBlock);
public Block resolveDirectLockTarget(Block clickedBlock);
public LockInfo findLock(Block block);
public LockInfo findManagedSignLock(Block block);
public AddPlayerResult addPlayerToLock(Sign preferredSign, LockInfo lock, String playerName);
public boolean canCreateMoreUsersSign(Block target);
public record LockInfo(Block signBlock, Block targetBlock, String owner, Set<String> allowedPlayers, LockType type) {}
```

From `src/main/java/ym/signLock/command/SignLockCommand.java`:
```java
private TargetedLock getTargetedLock(Player player) {
    Block targetBlock = player.getTargetBlockExact(6);
    if (!(targetBlock != null && targetBlock.getState() instanceof Sign sign)) {
        return null;
    }
    LockInfo lock = plugin.getLockService().findManagedSignLock(targetBlock);
    return lock == null ? null : new TargetedLock(sign, lock);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装 Phase 01 的 MockBukkit 回归底座并写出服务层红测</name>
  <read_first>
    - build.gradle
    - .planning/phases/01-lock-target-correctness/01-RESEARCH.md
    - .planning/phases/01-lock-target-correctness/01-VALIDATION.md
    - src/main/java/ym/signLock/service/LockService.java
    - src/main/java/ym/signLock/service/PlayerIdentityService.java
  </read_first>
  <files>build.gradle, src/test/java/ym/signLock/service/LockServiceCanonicalTargetTest.java, src/test/java/ym/signLock/service/LockServiceExtensionTest.java</files>
  <action>更新 `build.gradle`，加入 `org.junit.jupiter:junit-jupiter:6.0.3`、`org.mockbukkit.mockbukkit:mockbukkit-v1.21:4.108.0`、`org.mockito:mockito-junit-jupiter:5.23.0` 的 `testImplementation` 依赖，并配置 `test { useJUnitPlatform() }`。创建 `LockServiceCanonicalTargetTest` 与 `LockServiceExtensionTest`：前者覆盖 D-03 下双箱两半解析到同一 canonical target、`findLock`/`findPlacementTarget`/`resolveDirectLockTarget` 一致；后者覆盖 D-01、D-02、D-04 下扩展牌可挂在共享目标任一合法半边、`addPlayerToLock` 与 `canCreateMoreUsersSign` 不再依赖点击半边。测试用 MockBukkit 放置真实方块和告示牌，不要用 listener 级行为替代服务层断言。</action>
  <acceptance_criteria>
    - `build.gradle` 中存在 `useJUnitPlatform()` 和三条测试依赖声明。
    - `rg -n "class LockServiceCanonicalTargetTest|class LockServiceExtensionTest" src/test/java/ym/signLock/service` 能命中两个测试类。
    - `./gradlew.bat test --tests "ym.signLock.service.LockServiceCanonicalTargetTest" --tests "ym.signLock.service.LockServiceExtensionTest"` 能启动测试任务，失败点仅来自尚未实现的 Phase 01 共享目标行为，而不是测试基础设施缺失。
  </acceptance_criteria>
  <verify>
    <automated>./gradlew.bat test --tests "ym.signLock.service.LockServiceCanonicalTargetTest" --tests "ym.signLock.service.LockServiceExtensionTest"</automated>
  </verify>
  <done>测试基础设施可用，且 LOCK-01 / LOCK-02 / LOCK-03 对应的服务层红测已经落盘并能稳定运行。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在 LockService 内实现 canonical target 与共享目标扫描</name>
  <read_first>
    - src/main/java/ym/signLock/service/LockService.java
    - src/main/java/ym/signLock/service/PlayerIdentityService.java
    - src/test/java/ym/signLock/service/LockServiceCanonicalTargetTest.java
    - src/test/java/ym/signLock/service/LockServiceExtensionTest.java
    - .planning/phases/01-lock-target-correctness/01-CONTEXT.md
  </read_first>
  <files>src/main/java/ym/signLock/service/LockService.java</files>
  <behavior>
    - Test 1: 双箱左半与右半传入 `resolveDirectLockTarget` / `findPlacementTarget` 时返回同一 canonical block。
    - Test 2: 主锁牌贴在任意半边后，`findLock` 对双箱两半都返回同一 owner、同一 `targetBlock()`。
    - Test 3: 扩展牌放在共享目标任一半边时，`collectManagedSigns`、`addPlayerToLock`、`canCreateMoreUsersSign` 对整个双箱生效。
  </behavior>
  <action>重构 `LockService`，引入稳定且可重复的共享目标归一逻辑，按 D-03/D-04 为双箱先收集相关半边，再选出与点击侧无关的 canonical block；门的上下半归一继续保留。让 `findPlacementTarget`、`resolveDirectLockTarget`、`findLock`、`findManagedSignLock`、`collectManagedSigns`、`createMoreUsersSign`、`canCreateMoreUsersSign`、`readExtraAllowedPlayers` 全部复用同一 canonical target 语义，并保证 `LockInfo.targetBlock()` 始终保存 canonical block。不要引入新的持久化格式、不要把双箱逻辑散落回监听器，也不要加“单箱 1 / 双箱 2”这类硬限制，按 D-01、D-02 保留 `max-more-user-signs` 作为唯一数量上限。</action>
  <acceptance_criteria>
    - `rg -n "canonical|collectRelatedBlocks|findPlacementTarget|findManagedSignLock" src/main/java/ym/signLock/service/LockService.java` 能看出共享目标归一与扫描逻辑集中在 `LockService`。
    - 两个服务层测试类通过，不再依赖点击半边决定主锁或扩展牌的目标。
    - `LockInfo.targetBlock()` 在主锁与扩展牌相关路径上都指向同一个 canonical block。
  </acceptance_criteria>
  <verify>
    <automated>./gradlew.bat test --tests "ym.signLock.service.LockServiceCanonicalTargetTest" --tests "ym.signLock.service.LockServiceExtensionTest"</automated>
  </verify>
  <done>`LockService` 成为 Phase 01 的唯一锁目标语义源，LOCK-01 / LOCK-02 / LOCK-03 的共享目标行为已经被自动化测试锁住。</done>
</task>

</tasks>

<verification>
先执行服务层红测，再实现 `LockService` 直到两组服务测试转绿。完成后额外运行一次 `./gradlew.bat test --tests "ym.signLock.service.*"`，确认后续计划可直接基于该 canonical target 语义开展。
</verification>

<success_criteria>
- 支持的共享容器在服务层只有一个 canonical target 语义。
- 扩展牌授权读写不再受双箱半边差异影响。
- Wave 0 测试基础设施已建立，后续监听器计划不需要再补测试框架。
</success_criteria>

<output>
After completion, create `.planning/phases/01-lock-target-correctness/01-01-SUMMARY.md`
</output>
