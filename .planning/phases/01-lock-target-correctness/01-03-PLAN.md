---
phase: 01-lock-target-correctness
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/main/java/ym/signLock/listener/LockListener.java
  - src/test/java/ym/signLock/listener/LockListenerPlacementTest.java
  - src/test/java/ym/signLock/listener/LockListenerSignEditTest.java
autonomous: true
requirements:
  - LOCK-01
  - LOCK-02
  - LOCK-04
must_haves:
  truths:
    - "主锁牌与扩展牌贴在共享目标任一合法半边时，都会指向同一个保护目标。"
    - "只有锁主人或管理员能继续添加扩展牌，且扩展牌数量上限仍只受 `max-more-user-signs` 控制。"
    - "主人编辑主锁牌或扩展牌时，系统控制的头部与 owner 行不会被破坏。"
  artifacts:
    - path: "src/main/java/ym/signLock/listener/LockListener.java"
      provides: "贴牌流程与锁牌编辑结构保护"
      contains: "onSignChange"
    - path: "src/test/java/ym/signLock/listener/LockListenerPlacementTest.java"
      provides: "LOCK-01 / LOCK-02 贴牌回归测试"
      contains: "handleLockSignPlacement"
    - path: "src/test/java/ym/signLock/listener/LockListenerSignEditTest.java"
      provides: "LOCK-04 编辑结构保护回归测试"
      contains: "preserveManagedSignStructure"
  key_links:
    - from: "src/main/java/ym/signLock/listener/LockListener.java"
      to: "src/main/java/ym/signLock/service/LockService.java"
      via: "findPlacementTarget / findManagedSignLock / canCreateMoreUsersSign"
      pattern: "findPlacementTarget|findManagedSignLock|canCreateMoreUsersSign"
    - from: "src/test/java/ym/signLock/listener/LockListenerSignEditTest.java"
      to: "src/main/java/ym/signLock/listener/LockListener.java"
      via: "owner-only structure preservation regression"
      pattern: "signEditDeniedMessage|preserveManagedSignStructure"
---

<objective>
收口 Phase 01 的最后两个玩家可见入口: 主锁/扩展牌创建，以及锁牌编辑时的结构保护，确保共享目标语义真正落到贴牌与编辑体验上。

Purpose: 如果贴牌与编辑仍按半边或任意牌子局部判断，前两个计划建立的统一目标语义会在创建阶段立刻失效。
Output: 贴牌与编辑的 listener 回归测试，以及完成结构保护与共享目标修复后的 `LockListener`。
</objective>

<execution_context>
@C:/Users/32633/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/32633/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-lock-target-correctness/01-CONTEXT.md
@.planning/phases/01-lock-target-correctness/01-RESEARCH.md
@src/main/java/ym/signLock/listener/LockListener.java
@src/main/java/ym/signLock/service/LockService.java
@src/test/java/ym/signLock/listener/LockListenerInteractTest.java
@src/test/java/ym/signLock/listener/LockListenerInventoryOpenTest.java

<interfaces>
From `src/main/java/ym/signLock/listener/LockListener.java`:
```java
public void onSignChange(SignChangeEvent event);
private void handleLockSignPlacement(SignChangeEvent event);
private void handleMoreUsersPlacement(SignChangeEvent event);
private void preserveManagedSignStructure(SignChangeEvent event, LockInfo lock);
```

From `src/main/java/ym/signLock/service/LockService.java`:
```java
public Block findPlacementTarget(Block signBlock);
public LockInfo findManagedSignLock(Block block);
public boolean canManage(LockInfo lock, Player player);
public boolean canCreateMoreUsersSign(Block target);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为主锁/扩展牌贴牌与锁牌编辑补齐回归测试</name>
  <read_first>
    - src/main/java/ym/signLock/listener/LockListener.java
    - src/main/java/ym/signLock/service/LockService.java
    - .planning/phases/01-lock-target-correctness/01-CONTEXT.md
    - .planning/phases/01-lock-target-correctness/01-VALIDATION.md
  </read_first>
  <files>src/test/java/ym/signLock/listener/LockListenerPlacementTest.java, src/test/java/ym/signLock/listener/LockListenerSignEditTest.java</files>
  <action>创建 `LockListenerPlacementTest` 与 `LockListenerSignEditTest`。前者覆盖 LOCK-01 / LOCK-02：主锁牌可绑定到支持的共享目标，双箱任一合法半边都能作为主锁或扩展牌附着面；已有主锁时只有 owner/admin 能继续贴 `[更多用户]` 牌，且数量上限继续只走 `max-more-user-signs`，不要加入单箱/双箱硬限制。后者覆盖 LOCK-04 与 D-05/D-06：owner 右键编辑主锁牌或扩展牌时，header、owner 行会被强制恢复，授权名单行仍允许直接编辑；非 owner 编辑应收到 `signEditDeniedMessage()` 并取消事件。</action>
  <acceptance_criteria>
    - `rg -n "class LockListenerPlacementTest|class LockListenerSignEditTest" src/test/java/ym/signLock/listener` 能命中两个测试类。
    - 放置与编辑两组测试都覆盖双箱共享目标与 owner-only 编辑边界。
    - `./gradlew.bat test --tests "ym.signLock.listener.LockListenerPlacementTest" --tests "ym.signLock.listener.LockListenerSignEditTest"` 能运行到具体行为断言。
  </acceptance_criteria>
  <verify>
    <automated>./gradlew.bat test --tests "ym.signLock.listener.LockListenerPlacementTest" --tests "ym.signLock.listener.LockListenerSignEditTest"</automated>
  </verify>
  <done>LOCK-01 / LOCK-02 / LOCK-04 的贴牌与编辑红测已经建立，足以卡住 Phase 01 的结构性回归。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 收口 onSignChange 的共享目标贴牌与结构保护逻辑</name>
  <read_first>
    - src/main/java/ym/signLock/listener/LockListener.java
    - src/main/java/ym/signLock/service/LockService.java
    - src/test/java/ym/signLock/listener/LockListenerPlacementTest.java
    - src/test/java/ym/signLock/listener/LockListenerSignEditTest.java
    - .planning/phases/01-lock-target-correctness/01-CONTEXT.md
  </read_first>
  <files>src/main/java/ym/signLock/listener/LockListener.java</files>
  <behavior>
    - Test 1: 主锁牌贴在双箱任一合法半边时，`handleLockSignPlacement` 最终都绑定到同一个共享目标。
    - Test 2: 已有主锁后，只有 owner/admin 可以继续贴扩展牌，且扩展牌可附着在任意合法半边，符合 D-01 / D-02。
    - Test 3: 管理中的主锁牌与扩展牌被编辑时，系统控制的 header / owner 行会恢复，只有授权名单行可变，符合 D-05 / D-06。
  </behavior>
  <action>修改 `LockListener.onSignChange` 及其相关私有方法，使 `handleLockSignPlacement`、`handleAutomaticLockSignPlacement`、`handleMoreUsersPlacement` 和 `preserveManagedSignStructure` 全部以 Plan 01 的 `findPlacementTarget` / `findManagedSignLock` 为唯一来源。按 D-01/D-02 允许扩展牌挂在共享目标任一合法半边，并继续只受 `max-more-user-signs` 约束；按 D-05/D-06 保留 owner 直接编辑名单行的体验，但强制恢复主锁 header、owner 行和扩展牌 header。不要把授权管理收口到命令，不要修改牌子数据格式，也不要引入新的提示语整合工作。</action>
  <acceptance_criteria>
    - `LockListenerPlacementTest` 与 `LockListenerSignEditTest` 通过。
    - `rg -n "handleLockSignPlacement|handleMoreUsersPlacement|preserveManagedSignStructure" src/main/java/ym/signLock/listener/LockListener.java` 显示贴牌与编辑逻辑均已接入共享目标语义。
    - `./gradlew.bat test --tests "ym.signLock.listener.*"` 通过，且没有新增与 D-01..D-07 冲突的限制或行为变更。
  </acceptance_criteria>
  <verify>
    <automated>./gradlew.bat test --tests "ym.signLock.listener.LockListenerPlacementTest" --tests "ym.signLock.listener.LockListenerSignEditTest"</automated>
  </verify>
  <done>Phase 01 的主锁/扩展牌创建与锁牌编辑都已绑定到同一共享目标语义，LOCK-04 的结构保护也有自动化回归覆盖。</done>
</task>

</tasks>

<verification>
先完成两组 listener 红测，再修复 `onSignChange` 系列逻辑。Plan 03 结束后执行 `./gradlew.bat test --tests "ym.signLock.listener.*"` 作为 phase 内 listener 总回归。
</verification>

<success_criteria>
- 主锁牌、扩展牌、名单编辑三条路径都遵守 D-01..D-06。
- 贴牌流程不再依赖双箱点击半边。
- 锁牌编辑不会破坏主锁头、owner 行或扩展牌头部。
</success_criteria>

<output>
After completion, create `.planning/phases/01-lock-target-correctness/01-03-SUMMARY.md`
</output>
