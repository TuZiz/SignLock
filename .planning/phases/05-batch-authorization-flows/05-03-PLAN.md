---
phase: 05-batch-authorization-flows
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - src/main/java/ym/signLock/command/SignLockCommand.java
  - src/main/java/ym/signLock/config/SignLockConfig.java
  - src/test/java/ym/signLock/command/SignLockCommandBatchCompatibilityTest.java
  - src/test/java/ym/signLock/command/SignLockCommandCompatibilityTest.java
  - src/test/java/ym/signLock/gui/LockManagementBatchGuiTest.java
  - src/test/java/ym/signLock/listener/LockGuiBatchChatInputTest.java
autonomous: true
requirements:
  - BATCH-01
  - BATCH-02
  - BATCH-03
must_haves:
  truths:
    - "`/bl add` 与 `/bl remove` 在 Phase 5 后既支持多目标输入，也继续兼容旧单目标写法。"
    - "命令聚合反馈与 GUI 聚合反馈共享同一批量结果语义，而不是长出第二套分类。"
    - "Phase 5 closeout 会把批量 GUI、批量命令和旧命令兼容一并收口。"
  artifacts:
    - path: "src/main/java/ym/signLock/command/SignLockCommand.java"
      provides: "批量命令兼容入口"
    - path: "src/main/java/ym/signLock/config/SignLockConfig.java"
      provides: "批量结果消息模板"
    - path: "src/test/java/ym/signLock/command/SignLockCommandBatchCompatibilityTest.java"
      provides: "Phase 5 命令批量回归"
    - path: "src/test/java/ym/signLock/command/SignLockCommandCompatibilityTest.java"
      provides: "旧命令路径兼容回归"
  key_links:
    - from: "src/main/java/ym/signLock/command/SignLockCommand.java"
      to: "src/main/java/ym/signLock/service/LockBatchAuthorizationService.java"
      via: "命令多目标解析后统一走共享 batch service"
      pattern: "addPlayers|removePlayers"
    - from: "src/test/java/ym/signLock/command/SignLockCommandBatchCompatibilityTest.java"
      to: "src/main/java/ym/signLock/command/SignLockCommand.java"
      via: "batch add/remove parsing and aggregated feedback assertions"
      pattern: "handleAdd|handleRemove"
---

<objective>
完成 Phase 5 的命令兼容与 closeout：让 `/bl add`、`/bl remove` 支持多目标输入，并把 GUI 路径、命令路径和旧兼容路径一起锁进最终回归中。

Purpose: 把 Phase 5 从“GUI 已有批量能力”推进到“整套 owner 管理工作流都具备批量效率且兼容旧习惯”。
Output: 命令多目标解析、批量结果消息、命令兼容回归和 Phase 5 closeout 验证。
</objective>

<execution_context>
@C:/Users/32633/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/32633/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-batch-authorization-flows/05-CONTEXT.md
@.planning/phases/05-batch-authorization-flows/05-RESEARCH.md
@.planning/phases/05-batch-authorization-flows/05-VALIDATION.md
@.planning/phases/05-batch-authorization-flows/05-02-PLAN.md
@src/main/java/ym/signLock/command/SignLockCommand.java
@src/main/java/ym/signLock/config/SignLockConfig.java
@src/main/java/ym/signLock/service/LockBatchAuthorizationService.java
@src/test/java/ym/signLock/command/SignLockCommandCompatibilityTest.java

<interfaces>
Current command surface:
```java
if (args.length == 2 && args[0].equalsIgnoreCase("add")) { ... }
if (args.length == 2 && args[0].equalsIgnoreCase("remove")) { ... }
```

Planned compatibility target:
```java
/bl add Alice
/bl add Alice Bob Charlie
/bl add Alice,Bob,Charlie
/bl remove Alice Bob
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先写命令批量兼容与聚合反馈回归</name>
  <files>src/test/java/ym/signLock/command/SignLockCommandBatchCompatibilityTest.java, src/test/java/ym/signLock/command/SignLockCommandCompatibilityTest.java</files>
  <behavior>
    - Test 1: `/bl add` 与 `/bl remove` 接受多参数和逗号分隔输入，同时保留单参数旧写法。
    - Test 2: 命令路径会复用共享 parser / batch service，而不是在命令层手搓第二套规则。
    - Test 3: 聚合反馈正确区分成功、已存在、名单已满、未找到、owner 不可移除等结果。
    - Test 4: 旧 `SignLockCommandCompatibilityTest` 继续为 Phase 4 的 legacy contract 护航。
  </behavior>
  <action>新增 `SignLockCommandBatchCompatibilityTest`，把 Phase 5 的命令增强边界先锁成测试合同。测试必须覆盖单参数旧写法、多参数输入、逗号分隔输入、部分成功、部分失败、以及瞄准无效锁牌的失败路径。不要在这一步碰 GUI 文件；目标是先把命令兼容与聚合反馈口径钉住。</action>
  <verify>
    <automated>.\gradlew.bat test --tests ym.signLock.command.SignLockCommandBatchCompatibilityTest --tests ym.signLock.command.SignLockCommandCompatibilityTest</automated>
  </verify>
  <done>命令批量兼容回归已经存在，后续实现必须同时满足新批量 contract 和旧单玩家 contract。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 接入命令批量解析、聚合反馈并完成 Phase 5 closeout</name>
  <files>src/main/java/ym/signLock/command/SignLockCommand.java, src/main/java/ym/signLock/config/SignLockConfig.java, src/test/java/ym/signLock/command/SignLockCommandBatchCompatibilityTest.java, src/test/java/ym/signLock/command/SignLockCommandCompatibilityTest.java, src/test/java/ym/signLock/gui/LockManagementBatchGuiTest.java, src/test/java/ym/signLock/listener/LockGuiBatchChatInputTest.java</files>
  <behavior>
    - Test 1: 命令把多目标输入交给共享 parser / batch service，并输出聚合结果。
    - Test 2: 单目标旧写法仍然工作，不要求服主学习新命令。
    - Test 3: GUI 与命令对同一 mixed-result 批量操作给出一致的结果分类。
    - Test 4: Phase 5 相关测试一起通过，证明 GUI 和命令两条批量路径已闭环。
  </behavior>
  <action>增强 `SignLockCommand` 的 add/remove 参数解析，让它支持多目标输入但继续兼容旧写法；同时在 `SignLockConfig` 中补齐批量反馈所需的消息模板。命令层只做输入收集、锁目标解析和结果展示，真正的批量语义必须复用共享 batch service。最后按 `05-VALIDATION.md` 跑完整 Phase 5 相关回归，并把 manual verification 要点写进 SUMMARY / VERIFICATION。不要在这一步回头重做 GUI 结构；若命令实现暴露共享批量服务缺口，应该回补共享服务而不是在命令层开例外。</action>
  <verify>
    <automated>.\gradlew.bat test</automated>
  </verify>
  <done>Phase 5 的批量命令与 GUI 路径都已闭环，旧命令兼容仍为绿，且 closeout 验证可直接支撑后续 discuss Phase 6。</done>
</task>

</tasks>

<verification>
自动化完成后，补一次真实服手测：用 GUI 批量加人、批量移除，再用 `/bl add` 和 `/bl remove` 复现同一批 mixed-result 输入，确认两条路径的结果分类一致。
</verification>

<success_criteria>
执行完成后，批量授权能力已同时在 GUI 与命令路径落地，旧命令习惯不被打断，且 Phase 5 相关回归全部为绿。
</success_criteria>

<output>
After completion, create `.planning/phases/05-batch-authorization-flows/05-03-SUMMARY.md`
</output>
