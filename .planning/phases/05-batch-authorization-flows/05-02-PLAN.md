---
phase: 05-batch-authorization-flows
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/main/java/ym/signLock/gui/LockManagementGui.java
  - src/main/java/ym/signLock/gui/LockManagementGuiHolder.java
  - src/main/java/ym/signLock/gui/LockManagementGuiActionService.java
  - src/main/java/ym/signLock/gui/LockManagementPendingInputStore.java
  - src/main/java/ym/signLock/listener/LockGuiChatInputListener.java
  - src/main/java/ym/signLock/config/SignLockConfig.java
  - src/test/java/ym/signLock/gui/LockManagementBatchGuiTest.java
  - src/test/java/ym/signLock/listener/LockGuiBatchChatInputTest.java
autonomous: true
requirements:
  - BATCH-01
  - BATCH-02
  - BATCH-03
must_haves:
  truths:
    - "Phase 4 单页 GUI 在不引入分页和搜索的前提下，也能完成批量 add/remove 的主流程。"
    - "GUI 批量 add 继续走异步捕获、同步写回的安全边界。"
    - "GUI 批量 remove 采用选择后确认，而不是点击即删，降低误操作风险。"
  artifacts:
    - path: "src/main/java/ym/signLock/gui/LockManagementGui.java"
      provides: "批量选择与确认按钮布局"
    - path: "src/main/java/ym/signLock/gui/LockManagementGuiActionService.java"
      provides: "GUI 批量 add/remove 行为编排"
    - path: "src/main/java/ym/signLock/gui/LockManagementPendingInputStore.java"
      provides: "批量 add 待输入状态"
    - path: "src/test/java/ym/signLock/gui/LockManagementBatchGuiTest.java"
      provides: "GUI 批量交互回归"
    - path: "src/test/java/ym/signLock/listener/LockGuiBatchChatInputTest.java"
      provides: "批量输入桥接回归"
  key_links:
    - from: "src/main/java/ym/signLock/gui/LockManagementGuiActionService.java"
      to: "src/main/java/ym/signLock/service/LockBatchAuthorizationService.java"
      via: "GUI 批量 add/remove 复用共享服务"
      pattern: "addPlayers|removePlayers"
    - from: "src/main/java/ym/signLock/listener/LockGuiChatInputListener.java"
      to: "src/main/java/ym/signLock/gui/LockManagementPendingInputStore.java"
      via: "pending batch input capture + sync handoff"
      pattern: "consume|runTask"
---

<objective>
把共享批量语义真正接到 Phase 4 的单页 GUI 上：让主人可以一次输入多个玩家进行批量添加，并在同一页里选择多个玩家后确认批量移除。

Purpose: 在不推翻 Phase 4 结构的前提下，把 GUI 管理体验从单人操作升级成真正高效的批量工作流。
Output: GUI 批量 add/remove 交互、待输入状态扩展、批量结果聚合反馈与相应测试。
</objective>

<execution_context>
@C:/Users/32633/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/32633/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-batch-authorization-flows/05-CONTEXT.md
@.planning/phases/05-batch-authorization-flows/05-RESEARCH.md
@.planning/phases/05-batch-authorization-flows/05-VALIDATION.md
@.planning/phases/05-batch-authorization-flows/05-01-PLAN.md
@src/main/java/ym/signLock/gui/LockManagementGui.java
@src/main/java/ym/signLock/gui/LockManagementGuiHolder.java
@src/main/java/ym/signLock/gui/LockManagementGuiActionService.java
@src/main/java/ym/signLock/gui/LockManagementPendingInputStore.java
@src/main/java/ym/signLock/listener/LockGuiChatInputListener.java
@src/main/java/ym/signLock/service/LockBatchAuthorizationService.java

<interfaces>
Current GUI surfaces:
```java
public static final int[] PLAYER_SLOTS = { ... }
public void handleClick(Player player, LockManagementGuiHolder holder, int rawSlot)
public void handleChatInput(Player player, String rawInput)
```

Planned GUI extensions:
```java
public boolean isSelected(int rawSlot)
public void toggleSelectedPlayer(int rawSlot)
public Set<String> selectedPlayers()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先把 GUI 批量交互和选择态锁成测试合同</name>
  <files>src/test/java/ym/signLock/gui/LockManagementBatchGuiTest.java, src/test/java/ym/signLock/listener/LockGuiBatchChatInputTest.java</files>
  <behavior>
    - Test 1: 玩家槽位点击只切换 selected 状态，不立即写回删除。
    - Test 2: 确认移除按钮会把所有 selected 玩家交给共享 batch remove 服务，并回到刷新后的 GUI。
    - Test 3: 批量 add 输入支持 `Alice Bob,Charlie` 这种组合文本，并给出聚合反馈。
    - Test 4: 聊天监听仍然只负责捕获文本和切回同步调度，不在异步线程里做 Bukkit 写回。
  </behavior>
  <action>新增 GUI 批量交互测试，先把单页多选和批量输入桥接的边界固定下来。测试必须直接约束“选择态 vs 真正写回”的分离、批量 add 的多目标输入协议、聚合反馈而非刷屏消息、以及 chat bridge 的线程安全。不要在这一任务里先改命令解析；目标是让 GUI 的批量主流程先独立闭环。</action>
  <verify>
    <automated>.\gradlew.bat test --tests ym.signLock.gui.LockManagementBatchGuiTest --tests ym.signLock.listener.LockGuiBatchChatInputTest</automated>
  </verify>
  <done>GUI 批量交互已经有明确红绿测试合同，后续实现不需要再猜测选择态和输入桥接该怎么做。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 接入 GUI 批量 add/remove、选择态和聚合反馈</name>
  <files>src/main/java/ym/signLock/gui/LockManagementGui.java, src/main/java/ym/signLock/gui/LockManagementGuiHolder.java, src/main/java/ym/signLock/gui/LockManagementGuiActionService.java, src/main/java/ym/signLock/gui/LockManagementPendingInputStore.java, src/main/java/ym/signLock/listener/LockGuiChatInputListener.java, src/main/java/ym/signLock/config/SignLockConfig.java</files>
  <behavior>
    - Test 1: GUI 可视化 selected 状态，并通过确认按钮执行批量移除。
    - Test 2: 批量 add 仍通过聊天输入桥接，但最终走共享 batch service 和统一 parser。
    - Test 3: GUI 批量反馈以汇总形式输出，不为每个玩家刷单独消息。
  </behavior>
  <action>在保持单页模型的前提下，为 GUI 增加选择态、确认按钮、批量 add prompt 和聚合反馈。`LockManagementPendingInputStore` 应扩展为可表达批量 add 的输入状态；`LockManagementGuiActionService` 只做 UI 编排和共享 batch service 调用，不重新实现底层批量规则。不要在这一步修改 `/bl add`、`/bl remove` 的命令解析；也不要把 GUI 升级成分页、搜索、排序或更重的菜单系统。</action>
  <verify>
    <automated>.\gradlew.bat test --tests ym.signLock.gui.LockManagementBatchGuiTest --tests ym.signLock.listener.LockGuiBatchChatInputTest --tests ym.signLock.gui.LockGuiListenerClickSafetyTest</automated>
  </verify>
  <done>GUI 已具备批量 add/remove 主流程，且仍保持单页、低学习成本和主线程安全边界。</done>
</task>

</tasks>

<verification>
自动化完成后，至少补一次真实客户端手测：多选几个玩家后确认移除，确认不会误删；批量 add 输入成功和部分失败后，GUI 能稳定回到正确状态。
</verification>

<success_criteria>
执行完成后，锁主人可以在 Phase 4 的同一页 GUI 中完成批量添加与批量移除主流程，且不会因为新交互破坏主线程安全或批量语义一致性。
</success_criteria>

<output>
After completion, create `.planning/phases/05-batch-authorization-flows/05-02-SUMMARY.md`
</output>
