---
phase: 05-batch-authorization-flows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/ym/signLock/service/LockBatchAuthorizationService.java
  - src/main/java/ym/signLock/service/LockBatchTargetParser.java
  - src/test/java/ym/signLock/service/LockBatchAuthorizationServiceTest.java
  - src/test/java/ym/signLock/service/LockBatchTargetParserTest.java
autonomous: true
requirements:
  - BATCH-01
  - BATCH-02
  - BATCH-03
must_haves:
  truths:
    - "Phase 5 在 GUI 和命令接线之前，就已有可复用的批量 parser / result summary / orchestration 契约。"
    - "批量 add/remove 允许部分成功，但仍逐个复用 LockService 的单玩家写回语义。"
    - "后续 GUI 与命令不需要各自发明一套批量规则或消息分类。"
  artifacts:
    - path: "src/main/java/ym/signLock/service/LockBatchAuthorizationService.java"
      provides: "共享批量授权编排入口"
    - path: "src/main/java/ym/signLock/service/LockBatchTargetParser.java"
      provides: "兼容单目标和多目标的统一解析器"
    - path: "src/test/java/ym/signLock/service/LockBatchAuthorizationServiceTest.java"
      provides: "BATCH-01/BATCH-02/BATCH-03 服务层回归"
    - path: "src/test/java/ym/signLock/service/LockBatchTargetParserTest.java"
      provides: "批量输入协议与兼容边界回归"
  key_links:
    - from: "src/main/java/ym/signLock/service/LockBatchAuthorizationService.java"
      to: "src/main/java/ym/signLock/service/LockService.java"
      via: "逐个复用 addPlayerToLock/removePlayerFromLock"
      pattern: "addPlayerToLock|removePlayerFromLock"
    - from: "src/main/java/ym/signLock/service/LockBatchTargetParser.java"
      to: "src/main/java/ym/signLock/service/LockPlayerNameNormalizer.java"
      via: "parser 产物后续交给共享名字归一化路径"
      pattern: "normalize"
---

<objective>
为 Phase 5 建立共享批量语义基线：先把多目标输入解析、批量结果摘要和批量写回编排服务写清楚，并用 Wave 0 测试锁定后续 GUI 与命令都要复用的契约。

Purpose: 先证明批量模式的语义，再做入口接线，避免 GUI 和命令在 Wave 2 / Wave 3 各自长出不同规则。
Output: 共享 batch parser / orchestration service 代码骨架 + 服务层红绿测试。
</objective>

<execution_context>
@C:/Users/32633/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/32633/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-batch-authorization-flows/05-CONTEXT.md
@.planning/phases/05-batch-authorization-flows/05-RESEARCH.md
@.planning/phases/05-batch-authorization-flows/05-VALIDATION.md
@src/main/java/ym/signLock/service/LockService.java
@src/main/java/ym/signLock/service/LockPlayerNameNormalizer.java
@src/main/java/ym/signLock/gui/LockManagementGuiActionService.java
@src/main/java/ym/signLock/command/SignLockCommand.java

<interfaces>
Current single-target write surface from `LockService`:
```java
public AddPlayerResult addPlayerToLock(Sign preferredSign, LockInfo lock, String playerName)
public RemovePlayerResult removePlayerFromLock(Sign preferredSign, LockInfo lock, String playerName)
public boolean canManage(LockInfo lock, Player player)
```

Planned shared contracts:
```java
public final class LockBatchTargetParser {
    public List<String> parse(String rawInput)
}

public final class LockBatchAuthorizationService {
    public BatchAddSummary addPlayers(Player actor, Sign preferredSign, LockInfo lock, Collection<String> rawTargets)
    public BatchRemoveSummary removePlayers(Player actor, Sign preferredSign, LockInfo lock, Collection<String> rawTargets)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先写批量 parser 与 orchestration 的红绿合同</name>
  <files>src/test/java/ym/signLock/service/LockBatchAuthorizationServiceTest.java, src/test/java/ym/signLock/service/LockBatchTargetParserTest.java</files>
  <behavior>
    - Test 1: parser 同时接受单名字、空格分隔和逗号分隔输入，并对重复项做稳定去重。
    - Test 2: batch add 会保留 `ADDED`、`ADDED_WITH_EXTENSION`、`ALREADY_AUTHORIZED`、`NO_SPACE` 的分组结果，不做整批回滚。
    - Test 3: batch remove 会保留 `REMOVED`、`NOT_FOUND`、`OWNER_DENIED` 的分组结果，也允许部分成功。
    - Test 4: orchestration 不允许绕过 `canManage(...)`；非 owner/manage 不能执行批量写回。
  </behavior>
  <action>新增 `LockBatchTargetParserTest` 和 `LockBatchAuthorizationServiceTest`，先把 Phase 5 的共享批量语义锁成测试合同。测试必须明确覆盖兼容输入协议、名字去重、部分成功、扩展牌耗尽、owner 不可移除、以及权限边界。不要在这一任务里修改 GUI 或命令入口；目标是把 Wave 2 / Wave 3 都要复用的服务层口径固定下来。</action>
  <verify>
    <automated>.\gradlew.bat test --tests ym.signLock.service.LockBatchAuthorizationServiceTest --tests ym.signLock.service.LockBatchTargetParserTest</automated>
  </verify>
  <done>批量 parser 与 orchestration 的测试合同齐备，后续任何入口接线都必须围绕这套口径实现。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现共享批量 parser、结果摘要和 orchestration 服务</name>
  <files>src/main/java/ym/signLock/service/LockBatchAuthorizationService.java, src/main/java/ym/signLock/service/LockBatchTargetParser.java</files>
  <behavior>
    - Test 1: `LockBatchTargetParser` 返回有序、去重、剔除空项的目标列表，兼容旧单玩家输入。
    - Test 2: `LockBatchAuthorizationService` 逐个调用 `LockService` 单玩家入口，产出聚合结果摘要。
    - Test 3: 服务层不持有新的授权真源，也不直接改 sign 行文本。
  </behavior>
  <action>创建共享批量服务与解析器，只做 Phase 5 需要的最小稳定契约。`LockBatchAuthorizationService` 负责：校验管理权限、逐个调用 `LockService`、累计 grouped summary、保留执行顺序。不要在这一步写 GUI 状态、命令消息、槽位逻辑或新的配置面；也不要把聚合消息文案硬编码到 service 里，让 GUI 与命令后续各自决定展示方式。</action>
  <verify>
    <automated>.\gradlew.bat test --tests ym.signLock.service.LockBatchAuthorizationServiceTest --tests ym.signLock.service.LockBatchTargetParserTest</automated>
  </verify>
  <done>共享批量语义已经有实现基础，Wave 2 和 Wave 3 可以直接围绕该服务接线，而不是复制规则。</done>
</task>

</tasks>

<verification>
先跑服务层两组新测试；本计划结束时，不应存在第二套与之平行的 batch parser 或 batch summary 类型。
</verification>

<success_criteria>
执行完成后，Phase 5 已有共享批量 parser / summary / orchestration 基线，且这些基线显式满足 BATCH-01、BATCH-02、BATCH-03 的核心语义。
</success_criteria>

<output>
After completion, create `.planning/phases/05-batch-authorization-flows/05-01-SUMMARY.md`
</output>
